{
  "name": "subsite-static-search",
  "version": "0.1.0",
  "private": true,
  "description": "Static Search: browser-side Fuse.js search that works on a live WordPress site and in static HTML exports.",
  "license": "GPL-2.0-or-later",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "fuse.js": "7.5.0",
    "jsdom": "^26.0.0",
    "select2": "^4.0.13",
    "sortablejs": "^1.15.7"
  }
}
